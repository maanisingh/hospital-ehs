# Hospital EHS Environment Variables
# These should be set in Railway Dashboard for each service

# ===========================================
# MariaDB Configuration (from Railway MariaDB service)
# ===========================================
DB_HOST=mariadb.railway.internal
DB_PORT=3306
DB_USER=root
DB_ROOT_PASSWORD=your-mariadb-password
DB_NAME=hospital_ehs

# ===========================================
# Redis Configuration (from Railway Redis service)
# ===========================================
REDIS_HOST=redis.railway.internal
REDIS_PORT=6379

# ===========================================
# ERPNext Site Configuration
# ===========================================
SITE_NAME=hospital.local
ADMIN_PASSWORD=admin123

# ===========================================
# Railway Auto-Generated
# ===========================================
# PORT=8080 (auto-set by Railway)
# RAILWAY_PUBLIC_DOMAIN (auto-set by Railway)
